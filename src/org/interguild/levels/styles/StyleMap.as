package org.interguild.levels.styles {
	
	/**
	 * StyleMap keeps all of the arrays of StyleDefinitions and maps them
	 * to tile id's and class names.
	 * 
	 * NOTE: '.' is not a valid tile id.
	 */
	public class StyleMap extends Object {
		
		/**
		 * xml is the <objects> list. It will initialize all the objects.
		 */
		public function StyleMap(xml:XMLList) {
			
		}
	}
}
